identifier:        FancyCorp-Demo/tf-workspace-azure-webserver
working_directory: "split/network"
branch:            "combined-vs-split"

permissions:
  tf-fancycorp-devs: write
tags:
- webserver
- platform:azure
- environment:dev
- component:network
creds:
- azure
- hcp:viewer
tasks:
  - task: snyk
    enforcement: advisory
  - task: infracost
    enforcement: advisory
project: Webserver Dev
assessments_enabled: true

# Allow other workspaces to use this workspace's outputs
global_remote_state: true

# TODO
# downstream_workspaces
# - webserver-azure-dev-compute
# Or maybe this could be computed from the upstream_workspaces on the target workspaces
